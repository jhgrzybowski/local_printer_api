services:
  api:
    build:
      context: .
    image: local-printer-api:latest
    container_name: local-printer-api
    restart: unless-stopped
    ports:
      - "${BACKEND_HOST:-192.168.100.99}:8000:8000"
    environment:
      QUEUE_NAME: Canon_MG5350
      PRINTER_IP: 192.168.100.100
      BACKEND_HOST: 192.168.100.99
      BACKEND_PORT: "8000"
      TMP_DIR: /var/tmp/printer-backend
      DB_PATH: /var/lib/local-printer-api/app.db
      MAX_UPLOAD_MB: "50"
      PREVIEW_DPI: "110"
      CORS_ALLOWED_ORIGINS: http://192.168.100.99,http://192.168.100.99:80,http://ubuntu26-remote.local,http://ubuntu26-remote.local:80,http://drukarka.local,http://drukarka.local:80,http://192.168.100.99:5173,http://ubuntu26-remote.local:5173
    volumes:
      - /run/cups:/run/cups
      - printer-backend-tmp:/var/tmp/printer-backend
      - printer-backend-db:/var/lib/local-printer-api

volumes:
  printer-backend-tmp:
  printer-backend-db:
